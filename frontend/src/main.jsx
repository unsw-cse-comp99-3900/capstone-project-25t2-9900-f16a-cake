import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import './index.css';   // reset css to avoid the gap


const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
